(function(){
const {
    sDomF, ssF, amode, rg
} = window.b$l.atree({ ssFList: {
    amode2rgstate
}});
return;


function amode2rgstate( captured ){
    var { logic_phase, aspect, subessay } = amode;
    rg.foldPoints.cpivots.forEach( (fp,ppix) => {
        fp.rgShape.undisplay = true;
    });
    return captured;
}
})();