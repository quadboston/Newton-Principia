( function() {
    var {
        nspaste,
        commonsconf,
        stdMod,
    } = window.b$l.apptree({
        SUB_MODEL : 'proof-xix',
        stdModExportList :
        {
            init_conf,
        },
    });
    return;









    //====================================================
    // //\\ inits and sets config pars
    //====================================================
    function init_conf()
    {
        nspaste( stdMod.sconf, commonsconf );
    };
    //====================================================
    // \\// inits and sets config pars
    //====================================================

}) ();

