
//lost knowledge, see lemma 6

( function() {
    var {
        ns, sn, paste, capture, amode, rg, sDomF, ssD, ssF, fconf,
    } = window.b$l.apptree({
        ssFExportList :
        {
            amode2rgstate,
        },
    });
    setCapture();
    return;






    function setCapture()
    {
        /*
        paste( capture,
        {
            "idle": {
                    "idle": {
                    },
            },
        });
        */
    }


    function amode2rgstate( captured )
    {
        var { theorion, aspect, submodel, subessay } = amode;

        /*
        //fails:
        if( subessay === 'infinite-frequency-circle-based-curvature' ) {
            rg.B.pos[0] = 0.2;
            ccc( 'rese' )
        }
        */

        //----------------------------------
        // //\\ common values
        //----------------------------------
        //rg.A.undisplay              = false;
        //rg.B.undisplay              = false;
        //var media_scale = toreg( 'media_scale' )();
        //----------------------------------
        // \\// common values
        //----------------------------------


        //*****************************************************************************
        // //\\ lemma 6
        //*****************************************************************************
        /*
        if( fconf.sappId === "b1sec1lemma6" ) {
            captured = "reset-to-origin";
            if( theorion === 'claim' ) {
                 captured = 'L-equal-d';
            }
        }
        */
        //*****************************************************************************
        // \\// lemma 6
        //*****************************************************************************
        return captured;
    }

}) ();

